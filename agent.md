# 🧠 Walkie-Master Agent Architecture

**Technical Specification for LangGraph-based Robot Intelligence**

## 1. State Definition (`AgentState`)

หัวใจของระบบ Agentic Workflow คือการจัดการ State ที่ไหลเวียนอยู่ใน Graph ซึ่งเราได้ออกแบบให้ครอบคลุมบริบทของหุ่นยนต์ดังนี้:

| Key | Type | Description |
| --- | --- | --- |
| `messages` | `Annotated[List, add_messages]` | ประวัติการสนทนาและการทำ Function Calling (Persistent memory) |
| `current_pose` | `Dict[str, float]` | ตำแหน่งปัจจุบันจาก SDK (x, y, heading) |
| `last_observation` | `Dict[str, Any]` | ข้อมูลภาพล่าสุดที่ประมวลผลผ่าน `VisionDetector` |
| `mission_status` | `str` | สถานะปัจจุบันของภารกิจ (e.g., "FINDING_OBJECT", "MOVING") |

---

## 2. Graph Architecture (The Workflow)

ระบบใช้โครงสร้าง **Cyclic Graph** เพื่อให้ Agent สามารถตัดสินใจซ้ำได้จนกว่าภารกิจจะสำเร็จ:

### Nodes (The Workers)

1. **`agent` (The Decision Maker)**: รับ State ปัจจุบันไปให้ **Qwen3-8B** วิเคราะห์ผ่าน vLLM เพื่อตัดสินใจว่าจะตอบผู้ใช้ หรือเรียกใช้ Tools
2. **`tools` (The Executor)**: ทำหน้าที่รันฟังก์ชันจริงตามที่ LLM สั่ง เช่น สั่งหุ่นยนต์เดินผ่าน SDK หรือดึงภาพจาก Gazebo

### Edges (The Flow Control)

* **Conditional Edge**: ตรวจสอบหลังจากโหนด `agent` ว่า LLM มีการส่ง `tool_calls` มาหรือไม่?
* ถ้า **มี**: ส่งต่อไปยังโหนด `tools`
* ถ้า **ไม่มี**: จบการทำงานที่ `END` และตอบผู้ใช้



---

## 3. Prompting Strategy

เพื่อให้หุ่นยนต์มีความแม่นยำ (Deterministic) สูงที่สุดบน **RTX 5090** เราใช้เทคนิคดังนี้:

### System Instruction (The Core Rules)

* **Role**: เป็นหุ่นยนต์บริการระดับมืออาชีพในการแข่งขัน RoboCup@Home
* **Execution Logic**:
1. หากต้องการเคลื่อนที่ ให้ใช้ `move_to_coordinates`
2. หากต้องการหาของ ต้องเรียก `get_current_view` ก่อนเสมอเพื่ออัปเดตภาพล่าสุด
3. ตามด้วย `analyze_and_store_objects` เพื่อบันทึกข้อมูลลง Memory


* **Constraints**: ห้ามจินตนาการพิกัดเอง หากไม่รู้ให้ใช้ Tool หรือถามผู้ใช้

### Zero-Inference Policy

เราล็อคค่า `temperature: 0.0` ใน `QwenAgentClient` เพื่อให้แน่ใจว่าเมื่อได้รับคำสั่งเดิม หุ่นยนต์จะตัดสินใจเรียก Tool เดิมทุกครั้ง

---

## 4. Integrated Tools Specification

รายการเครื่องมือที่ Agent สามารถเข้าถึงได้ในปัจจุบัน:

### 📍 Navigation Tools

* **`move_to_coordinates(x, y, heading)`**: เชื่อมต่อกับ Nav2 ผ่าน Walkie-SDK คืนค่าสถานะ "SUCCEEDED" หรือ "FAILED"

### 👁️ Vision & Memory Tools (The Pipeline)

* **`get_current_view()`**: ดึงภาพสดจาก Gazebo/Camera มาเก็บใน Buffer
* **`analyze_and_store_objects()`**:
* Input: ภาพจาก Buffer
* Processing: YOLO11 Detection
* Output: บันทึกพิกัดและ Label ลงใน **WalkieDB**



---

## 5. Decision Mapping (Next Steps Planning)

เรากำลังก้าวเข้าสู่ **Phase 2** โดยมีแผนการปรับปรุงระบบคิดดังนี้:

1. **Entity Linking Implementation**: ปรับปรุง Node `tools` ให้มีความฉลาดพอที่จะเช็คว่าวัตถุที่เห็นใหม่ คือวัตถุเดิมใน Database หรือไม่ (ป้องกัน Data Redundancy)
2. **Self-Correction Loop**: เพิ่มโหนดตรวจสอบสถานะ (Validation Node) เพื่อเช็คว่าถ้าหุ่นยนต์เดินชนอุปสรรค จะต้องลองเส้นทางใหม่หรือแจ้งผู้ใช้ทันที
3. **HPC Optimization**: ใช้ประโยชน์จาก RTX 5090 ในการรันโมเดล Vision ที่ใหญ่ขึ้น (เช่น PaliGemma 2) เพื่อทำ VLM Reasoning สำหรับภารกิจที่ซับซ้อน

---

## 🛠️ Internal Developer Notes

* **LLM Model**: `Qwen/Qwen3-8B` (Quantized via bitsandbytes 4-bit)
* **Tool Parser**: ใช้ `hermes` ใน vLLM เพื่อความแม่นยำในการแกะ JSON
* **Graph State**: ต้องระวังการใช้ `None` ในข้อความตอบกลับ ให้ใช้ `AIMessage` ว่างแทนเสมอ

---
