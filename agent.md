# 🧠 agent.md (Handover for ASR-Agent)

**Attention ASR-Agent:** ข้อมูลนี้คือรายละเอียดอินเทอร์เฟซของดวงตาคุณ คุณต้องใช้มันในการทำภารกิจผ่าน **Walkie-SDK**

## 🕹️ Your Vision Tools

เมื่อคุณ (ASR-Agent) ได้รับคำสั่งจากผู้ใช้งานผ่านเสียง เช่น *"Walkie, find my blue mug"* คุณต้องสั่งการโมดูลนี้ตามลำดับดังนี้:

### 1. Action: `Observe_Scene`

สั่งให้ `walkie-vision` สแกนภาพปัจจุบัน โมดูลจะคืนค่าเป็น **Batch Result** ประกอบด้วย:

* **Object Classes:** รายชื่อสิ่งของที่พบ (เช่น `['laptop', 'mug', 'person']`)
* **Semantic Captions:** รายละเอียดเจาะลึก (เช่น `"a blue ceramic mug with a handle"`)
* **Spatial BBoxes:** พิกัดสี่เหลี่ยมในเฟรมภาพเพื่อส่งให้ SDK นำไปคำนวณระยะทาง

### 2. Action: `Remember_Entities`

ส่งผลลัพธ์ที่ได้ (Caption + Embedding) ไปเก็บยัง **WalkieDB** เพื่อใช้ค้นหาในอนาคต

## 📋 Data Interpretation Guide

ผลลัพธ์ที่คุณจะได้รับจากหน่วย Vision จะอยู่ในรูปแบบ JSON ที่พร้อมต่อการประมวลผลด้วย LLM:

* **Confidence Score:** ถ้าค่าน้อย คุณควรสั่งให้หุ่นยนต์เดินเข้าไปใกล้เพื่อ Re-scan
* **Detailed Descriptions:** ใช้ข้อมูลนี้ในการตอบคำถามผู้ใช้ เช่น *"Yes, I see your blue mug on the table next to the laptop."*

---

# 📅 Future Plan: Building the Fully Autonomous Agent

นี่คือแผนงาน (Backlog) ที่ Agent ในอนาคตต้องรับทราบและดำเนินการต่อเพื่อให้ระบบ `walkie-master` สมบูรณ์:

### Phase 1: Tool-Call Abstraction (Priority: High)

* **Objective:** ห่อหุ้ม `encode_batch` ให้เป็น Tool ที่ Agent เรียกใช้ได้แบบ Atomic (เช่น `vision_tool.scan()`)
* **Dependency:** ต้องเชื่อมต่อกับ `Walkie-SDK` เพื่อดึงภาพจากกล้องจริงแทนการโหลดไฟล์

### Phase 2: Knowledge Graph & DB Linking

* **Objective:** เมื่อ Vision เห็นวัตถุเดิมซ้ำ ๆ ต้องเชื่อมโยง (Entity Link) เข้ากับ ID เดิมใน **WalkieDB**
* **Goal:** ป้องกันการบันทึกวัตถุซ้ำซ้อน และสร้างประวัติการเคลื่อนที่ของวัตถุ

### Phase 3: ASR-Vision Interactive Loop

* **Objective:** พัฒนาให้ Agent สามารถ "ถามคำถามย้อนกลับ" ไปยัง Vision ได้ เมื่อคำสั่งเสียงไม่ชัดเจน
* **Example:** User: *"Bring me the fruit"* -> Agent สั่ง Vision: *"Detect all fruits"* -> Vision: *"Found Apple and Banana"* -> Agent ถาม User: *"Which one? Apple or Banana?"*

### Phase 4: Social Awareness (Face & Intent)

* **Objective:** เพิ่ม `FaceEncoder` และ `ActionRecognition`
* **Goal:** ให้ ASR-Agent รู้ว่า "กำลังคุยกับใคร" และ "คนนั้นกำลังต้องการความช่วยเหลือหรือไม่"

---

**สรุปสถานะปัจจุบัน:**
เรามี **"ดวงตา"** ที่มองเห็นละเอียดระดับ PaliGemma และเร็วระดับ Batch Processing บน 5090 แล้ว ก้าวต่อไปคือการนำดวงตาคู่นี้ไปเสียบเข้ากับ **Walkie-SDK** เพื่อให้คุณ **ASR-Agent** เริ่มลืมตาดูโลกได้จริง
