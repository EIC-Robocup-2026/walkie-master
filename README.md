# 🤖 Walkie-Master: RoboCup@Home 2026 Edition

**Walkie-Master** คือหัวใจหลัก (Central Nervous System) ของหุ่นยนต์เซอร์วิสอัจฉริยะ "Walkie" ที่พัฒนาขึ้นสำหรับการแข่งขัน **RoboCup@Home 2026** ระบบนี้บูรณาการความสามารถของ LLM Reasoning เข้ากับระบบ Vision และ Navigation ขั้นสูง เพื่อมอบประสบการณ์การโต้ตอบที่ลื่นไหลและแม่นยำ

---

### 🧠 System Architecture: The Agentic Brain

หัวใจของระบบคือ **Agent Orchestrator** ที่เปลี่ยนคำสั่งเสียงจากมนุษย์ให้กลายเป็นแผนการทำงาน (Action Plan) ที่เป็นลำดับขั้นตอน โดยใช้โมเดล **Qwen3-8B** ผ่านเทคนิค Function Calling

* **Standard Operating Procedures (SOP):** หุ่นยนต์ถูกโปรแกรมด้วยกฎเหล็กเพื่อให้ทำงานอย่างเป็นระบบ เช่น การมองวัตถุต้องเริ่มจาก `get_current_view` ➔ `analyze_and_store_objects` เสมอ
* **Deterministic Reasoning:** ใช้ระบบ System Prompt และ Few-shot Examples เพื่อควบคุมให้ AI คิดอย่างมีเหตุผล (Thought Process) ก่อนตัดสินใจลงมือทำ
* **Dynamic Tool Schema:** ระบบดึงข้อมูลจาก Python Docstrings มาสร้างเป็น Tool Definition โดยอัตโนมัติ ทำให้การขยายความสามารถทำได้แบบรวดเร็วและไม่ซับซ้อน

---

### 🚀 Key Features

* **Intelligent Navigation:** สั่งการเดินผ่านพิกัด  หรือระบุชื่อสถานที่ผ่านฐานข้อมูลความจำ
* **Neural Vision Pipeline:** ตรวจจับและแยกแยะวัตถุด้วย **YOLO11** และ **PaliGemma** บนฮาร์ดแวร์ประสิทธิภาพสูง (RTX 5090)
* **Long-term Spatial Memory:** บันทึกตำแหน่งและลักษณะวัตถุลงใน **ChromaDB (Vector Database)** เพื่อให้หุ่นยนต์ "จำได้" ว่าเคยเห็นอะไรที่ไหน
* **Context-Aware Interaction:** สามารถดึงบริบทจากบทสนทนา (เช่น "Sofa is at 1.2, 0.5") มาใช้ในการคำนวณแผนการเดินทางได้ทันที

---

### 🛠️ Hardware Requirements & Environment

* **Compute:** NVIDIA RTX 5090 (Recommended for Vision Inference)
* **Python:** 3.12+ (Managed via `uv`)
* **Database:** ChromaDB (Vector Store)
* **SDK:** Walkie-SDK (Custom Hardware Interface)

---

### 🧪 Testing & Development

เราให้ความสำคัญกับ **Test-Driven Development (TDD)** เพื่อให้มั่นใจว่า "สมอง" ของหุ่นยนต์ทำงานได้ถูกต้องก่อนลงสนามจริง

**การติดตั้ง Dependency:**

```bash
uv sync

```

**การทดสอบระบบวางแผน (Orchestration Testing):**
เรามีระบบ Mocking ที่ช่วยให้ทดสอบ Logic ทั้งหมดได้โดยไม่ต้องต่อหุ่นยนต์จริง:

* **Navigation Logic Test:** ตรวจสอบความถูกต้องของการคำนวณพิกัดจากคำสั่ง
`uv run pytest apps/asr-agent/src/asr_agent/tests/test_navigation.py -v -s`
* **Vision Pipeline Test:** ทดสอบการดึงภาพจริงจากโฟลเดอร์มาประมวลผลจนถึงขั้นตอนลงฐานข้อมูล
`uv run pytest apps/asr-agent/src/asr_agent/tests/test_vision_real_data.py -v -s`

---

### 📁 Project Structure

* `apps/asr-agent/`: สมองส่วนหน้าและระบบตัดสินใจ (LLM Orchestrator)
* `packages/walkie-sdk/`: ระบบควบคุมฮาร์ดแวร์พื้นฐาน (Drive, Camera, Status)
* `packages/walkie-vision/`: ระบบประมวลผลภาพ (YOLO11, Segment Anything)
* `packages/walkie-db/`: ระบบฐานข้อมูลความจำระยะยาว
* `data/images/scenes/`: คลังภาพจำลองสำหรับใช้ในระบบ Integration Test

---
