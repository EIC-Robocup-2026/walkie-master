# 🧠 Agent Orchestrator & Reasoning Logic

เอกสารฉบับนี้อธิบายโครงสร้าง **"สมอง"** ของ Walkie ที่ทำหน้าที่เปลี่ยนคำสั่งภาษาธรรมชาติ (Natural Language) ให้กลายเป็นชุดคำสั่งที่หุ่นยนต์สามารถปฏิบัติได้จริง ผ่านกระบวนการคิดที่เป็นระบบและการวางแผนล่วงหน้า (Multi-step Planning)

---

### 1. The Orchestrator Concept

**Agent Orchestrator** เปรียบเสมือนวาทยกรที่ควบคุมโมดูลต่าง ๆ ของหุ่นยนต์ โดยมีหลักการทำงานดังนี้:

* **Centralized Decision Making:** ใช้โมเดล **Qwen3-8B (Function Calling Optimized)** เป็นแกนกลางในการตัดสินใจ
* **Context Management:** รักษาประวัติการสนทนา (Conversation History) และบริบทของสภาพแวดล้อม (Context Awareness) เพื่อใช้ในการคำนวณพิกัดหรือระบุวัตถุ
* **Dynamic Tool Binding:** แปลง Docstrings จาก Python functions เป็น JSON Schemas โดยอัตโนมัติ เพื่อให้ AI เข้าใจความสามารถของหุ่นยนต์ในรูปแบบ Single Source of Truth

---

### 2. Standard Operating Procedures (SOP)

เพื่อให้การทำงานในสนามแข่ง **RoboCup@Home 2026** เป็นไปอย่างแม่นยำและลดความล่าช้า (Latency) เราได้กำหนดขั้นตอนปฏิบัติมาตรฐาน (SOP) ไว้ใน System Prompt ดังนี้:

| ภารกิจ (Mission) | ลำดับการเรียกใช้เครื่องมือ (Execution Flow) | เหตุผล (Rationale) |
| --- | --- | --- |
| **การมอง (Look)** | `get_current_view` ➔ `analyze_and_store_objects` | ต้องถ่ายภาพก่อนจึงจะประมวลผล YOLO/PaliGemma ได้ |
| **การไป (Go)** | `move_to_coordinates(x, y, heading)` | ใช้พิกัด  ที่ระบุหรือดึงจากความจำ |
| **การหา (Search)** | `search_memory` ➔ `move_to_target` ➔ `Look` | ตรวจสอบตำแหน่งล่าสุดก่อนจะเคลื่อนที่ไปดูจริง |

---

### 3. Reasoning & Thought Process

ก่อนที่ Walkie จะเรียกใช้ Tool ใด ๆ หุ่นยนต์จะแสดงกระบวนการคิดผ่าน **"Thought"** เพื่ออธิบายเหตุผลเบื้องต้น:

1. **Input:** "Walkie, where is the blue mug?"
2. **Thought:** "The user wants to find a specific object. I must first check my internal database to see its last recorded position."
3. **Action:** Calls `search_memory(query="blue mug")`

---

### 4. Technical Specifications

* **Reasoning Engine:** Qwen3-8B (vLLM Backend)
* **Vision Model:** PaliGemma (VLM) + YOLO26 (Detection)
* **Inference Hardware:** NVIDIA RTX 5090
* **Spatial Mapping:** พิกัดอ้างอิงแบบ Cartesian  ในหน่วยเมตร และ Heading  ในหน่วยเรเดียน

---

### 🧪 Planning-Only Verification

ในขั้นตอนการพัฒนาและการทำ **Integration Test** เราใช้ระบบ **Planning-Only** เพื่อตรวจสอบความถูกต้องของ Logic:

* **Validation:** ตรวจสอบว่า AI สั่งรัน Tools ได้ครบถ้วนตาม SOP หรือไม่ (เช่น สั่ง Look แล้วต้องมีทั้ง Capture และ Analyze)
* **Mocking Environment:** ใช้การจำลองผลลัพธ์จาก Vision และ Navigation เพื่อเช็คว่า AI สามารถนำข้อมูลเหล่านั้นมาวางแผนในขั้นตอนถัดไปได้ถูกต้อง

---
