# 🤖 Walkie-Master: RoboCup@Home 2026

**Autonomous Service Robot powered by LangGraph, vLLM, and ROS 2**

---

## 📋 Project Overview

**Walkie-Master** คือระบบปฏิบัติการหุ่นยนต์อัจฉริยะที่ออกแบบมาเพื่อการแข่งขัน **RoboCup@Home 2026** โดยเน้นการประมวลผลแบบ Local High-Performance บนขุมพลัง **RTX 5090** ระบบใช้สถาปัตยกรรมแบบ **Agentic Workflow** เพื่อเชื่อมโยงการรับรู้ (Vision), การฟัง (ASR), และการสั่งการเคลื่อนที่ (Navigation) เข้าด้วยกันอย่างราบรื่น

---

## 🏗️ System Architecture & Packages

โปรเจกต์ถูกแบ่งออกเป็น Modular Packages เพื่อความง่ายในการทดสอบและนำไปใช้ซ้ำ:

### 1. 📦 Packages (Core Modules)

* **`walkie-sdk`**: หัวใจหลักในการเชื่อมต่อหุ่นยนต์ผ่าน **ROSTransportInterface** รองรับทั้ง `rosbridge` และ `zenoh` มี Module ย่อยอย่าง `Navigation` ที่ควบคุม Nav2 (Go-to, Stop, Cancel)
* **`walkie-vision`**: ระบบดวงตาของหุ่นยนต์ ประกอบด้วย `VisionDetector` (YOLO11/PaliGemma) และ `CameraBridge` สำหรับดึงภาพจาก Gazebo Simulation หรือกล้องจริง
* **`walkie-db`**: ระบบความจำระยะยาว (Long-term Memory) โดยใช้ `AgentIntegration` บันทึกตำแหน่งและคุณลักษณะของวัตถุ (Embeddings) ลงใน Vector Database

### 2. 🧠 Apps (Intelligence Layer)

* **`asr-agent`**: แอปพลิเคชันหลักที่จัดการ **LangGraph Workflow**
* **ASR**: ใช้ `Faster-Whisper` (distil-large-v3) แปลงเสียงเป็นข้อความบน GPU
* **Brain**: ใช้ `Qwen3-8B` (Host ผ่าน vLLM) ทำ Function Calling เพื่อตัดสินใจใช้ Tools
* **Graph**: ควบคุม State ของการสนทนาและการทำ Mission



---

## 🔄 System Main Flow (Pipeline)

ลำดับการทำงานจากเสียงสั่งการสู่การกระทำของหุ่นยนต์:

1. **Audio Input**: รับเสียงผ่านไมโครโฟน -> `Faster-Whisper` ประมวลผลเป็น Text
2. **LLM Reasoning**: Text ถูกส่งเข้าสู่ `LangGraph` -> `Qwen3` วิเคราะห์ Intent
3. **Tool Selection**: หากต้องขยับหรือหาของ Agent จะเรียก Tools:
* `move_to_coordinates`: สั่ง SDK ให้หุ่นยนต์เดิน
* `get_current_view`: ดึงภาพจาก Gazebo
* `analyze_and_store_objects`: รัน YOLO และบันทึกลง WalkieDB


4. **Action Execution**: Tools ทำงานร่วมกับ Hardware/Simulation -> คืนค่าผลลัพธ์กลับมาให้ Agent
5. **Final Response**: Agent สรุปสิ่งที่ทำสำเร็จและตอบโต้กลับหาผู้ใช้

---

## 🧪 Testing Strategy (Verification Layers)

เราวางระบบการทดสอบไว้หลายระดับเพื่อความมั่นใจก่อนลงสนามจริง:

* **Smoke Test**: ตรวจสอบการโหลดโมเดล (YOLO, Whisper) และการเชื่อมต่อพื้นฐาน (SDK, DB, LLM Port 8000)
* **LLM Logic Test**: ตรวจสอบความแม่นยำของ Qwen3 ในการตัดสินใจเรียก Tool (Deterministic Test)
* **Tool Unit Test**: ทดสอบ Logic ภายในของแต่ละ Tool เช่น การคำนวณพิกัด Navigation
* **Vision Pipeline Test**: ทดสอบ Flow ต่อเนื่อง ตั้งแต่ถ่ายภาพจาก Sim จนถึงการบันทึกลง Database

---

## 🚀 Future Planning & Roadmap

### Phase 1: Foundation (Current State) 🚧

* [x] สร้าง Modular Packages (SDK, Vision, DB)
* [x] ติดตั้ง vLLM พร้อม Quantization (4-bit AWQ/Bitsandbytes)
* [x] วางโครงสร้าง LangGraph และ Tool Integration เบื้องต้น

### Phase 2: Knowledge & Refinement (Next Step) 🎯

* [ ] ทำ **Entity Linking** ใน WalkieDB (เช็ควัตถุซ้ำก่อนบันทึก)
* [ ] พัฒนา **Vision-Language Loop** (ให้ Agent ถามกลับเมื่อคำสั่งกำกวม)
* [ ] ปรับปรุง Prompt ให้รองรับสถานการณ์ซับซ้อนใน RoboCup (เช่น การหาคน)

### Phase 3: Hardware Integration 🤖

* [ ] ทดสอบบนหุ่นยนต์จริงผ่าน Zenoh Transport
* [ ] ปรับแต่ง Performance ให้ Latency ของระบบ ASR-to-Action ต่ำกว่า 1 วินาที

---

## 🛠️ Usage Quick Start

1. **Serve LLM**: `bash scripts/serve_llm.sh`
2. **Run Tests**: `uv run pytest apps/asr-agent/src/asr_agent/tests/ -v`
3. **Launch Agent**: `uv run python apps/asr-agent/main.py`

---
